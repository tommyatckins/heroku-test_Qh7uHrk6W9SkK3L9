BASE_URL = 'https://google.com'

def sum_numbers(nums):
    total = 0
    for num in nums:
        total += num
    return total


class main:
    def _sum(nums):
        return sum(nums)
    
    def create_user(username):
        return username == 'admin'
    
    def best_name(name):
        return name == 'ali'

def str_len(_str):
    return len(_str)
